use cget, cput;
use cast_to;



decfun solve($n:int): do {
    dec $aaa: int = 5+3*5;

    loond((1 < 2) && !(1!=1)) {
        dec $aL: int = 5+3*5;
        cond( 1 == 2 ){
            dec $aC: int = 5+3*5;
            loond(1 < 2) {
                dec $aL: int = 5+3*5;

                return $aL;
            }
        }
    }

    return $aaa;
}